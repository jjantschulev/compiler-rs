import a as c, b, from "path/to/file";

type MyType = {
    name: string[3],
    age: int,
    fish: (&int) => void
}[][3];

while 1 {
    type X = Foo;
}

let person: MyType = {
    name: ["jordan", "i", "jantschulev"],
    age: 123
};

let printf: (string) => void = (var: string): int => {
    var = var + 1;
    var += 1;
};

loop {
    ((((({v : person.p.age + 23})))));
}

/* 
import sub from "lib/subtract"

type X = {
    name: string,
    age: int,
    print: (printer: &any) => X,
}

let add = (lhs: int, rhs: int): int => {
    let sum: int = lhs + rhs;
    return sum;
}
 
add(123, 345)

*/