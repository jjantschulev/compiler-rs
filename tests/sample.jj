{
    name: string,
    age: (int, &float),
    go_swimming: () => {
        water: bool,
        temp: Temperature,
        measure: (&int, &int) => float,
    },
}

/* 
import sub from "lib/subtract"

type X = {
    name: string,
    age: int,
    print: (printer: &any) => X,
}

let add = (lhs: int, rhs: int): int => {
    let sum: int = lhs + rhs;
    return sum;
}
 
add(123, 345)

*/