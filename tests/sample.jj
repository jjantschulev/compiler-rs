import sub from "lib/subtract";

/* 
import sub from "lib/subtract"

type X = {
    name: string,
    age: int,
    print: (printer: &any) => X,
}

let add = (lhs: int, rhs: int): int => {
    let sum: int = lhs + rhs;
    return sum;
}
 
add(123, 345)

*/